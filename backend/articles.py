# This file handles saving, retrieving, and deleting articles for logged-in users (Flask Blueprint)
# TODO: write POST /articles/save route (save to DB)
# TODO: write GET /articles/saved route (get user's saved articles)
# TODO: write DELETE /articles/<id> route (delete saved article)
